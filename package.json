{
  "name": "structured-table-results",
  "version": "0.0.1",
  "description": "Grapical editing and display roll table results",
  "main": "index.js",
  "scripts": {
    "build": "tsc --build",
    "postbuild": "cpy module.json dist/",
    "build:dev": "tsc --build  --clean",
    "postbuild:dev": "cpy module.json dist/",
    "watch": "tsc --watch",
    "pretest": "npm run build:dev",
    "test": "jasmine"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/jasmine": "^5.1.4",
    "@types/node": "^22.5.5",
    "cpy-cli": "^5.0.0",
    "jasmine": "^5.3.0",
    "jasmine-ts": "^0.4.0",
    "ts-node": "^10.9.2",
    "tsc-hooks": "^1.1.2",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "foundry-pc-types": "gitlab:foundry-projects/foundry-pc/foundry-pc-types"
  }
}
